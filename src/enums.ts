export enum QuaireComponentType {
  INPUT = 'INPUT',
  SINGLE_SELECT = 'SINGLE_SELECT',
  MULTI_SELECT = 'MULTI_SELECT',
  RANGE_SLIDER = 'RANGE_SLIDER',
}

export enum QuaireValidationError {
  REQUIRED = 'REQUIRED',
}
